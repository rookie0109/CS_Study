## 输出
1.四舍五入：
```cpp
#include <iomanip>
std::cout << std::fixed << std::setprecision(2) << value << std::endl;
#include <cmath>
std::round(value * 100) / 100
```

## 输入
```cpp
// 适用于字符串转换大小写
std::tolower
std::toupper
std::cin.ignore()   // 忽略一个字符
std::cin.ignore(100, '\n')    //  忽略100个字符直到换行符
```

```cpp
std::sort()
std::greater<>表示从大到小排序
std::priority_queue<>
std::greater<>表示从最小堆
```



